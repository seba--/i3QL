/**
 * Package for database predicates that expect exact result sets. That is, 
 * every result have to unify with the query (and the set of results is 
 * complete).
 */
package saere.database.predicate.full;
